@model CreateProductVm

<div class="container">
    <h1>Create Product</h1>
    <form method="post" enctype="multipart/form-data">
        <div class="row">
            <div class="mb-3 col-md-3">
                <label asp-for="Name" class="form-label">Name</label>
                <input asp-for="Name" class="form-control">
                <span asp-validation-for="Name" style="color:red"></span>
            </div>
            <div class="mb-3 col-md-3">
                <label asp-for="SKU" class="form-label">SKU</label>
                <input asp-for="SKU" class="form-control">
                <span asp-validation-for="SKU" style="color:red"></span>
            </div>
            <div class="mb-3 col-md-3">
                <label asp-for="Price" class="form-label">Price</label>
                <input asp-for="Price" class="form-control">
                <span asp-validation-for="Price" style="color:red"></span>
            </div>
        </div>
        <div class="row align-items-center">
            <div class="mb-3 col-md-6">
                <label asp-for="Description" class="form-label">Description</label>
                <textarea class="form-control" asp-for="Description" rows="4"></textarea>
                <span asp-validation-for="Description" style="color:red"></span>
            </div>
            <div class="mb-3 col-md-3">
                <label asp-for="CategoryId" class="form-label">Categories</label>
                <select class="form-select" asp-for="CategoryId" asp-items="new SelectList(ViewBag.Categories , nameof(Category.Id),nameof(Category.Name))">
                    <option selected>Open this select menu</option>
                    <span asp-validation-for="CategoryId" style="color:red"></span>
                </select>
            </div>
            <div class="mb-3 col-md-3">
                <label asp-for="TagIds" class="form-label">Tags</label>
                <select class="form-select" asp-for="TagIds" asp-items="new SelectList(ViewBag.Tags, nameof(Tag.Id) , nameof(Tag.Name))" multiple aria-label="multiple select example">
                   
                    <span asp-validation-for="TagIds" style="color:red"></span>
                </select>
            </div>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>

@* @foreach (var item in ViewBag.Categories)
                    {
                        <option value="@item.Id">@item.Name</option>
                    } *@

@* <div class="container">
    <h1>Create Product</h1>
    <form method="post" enctype="multipart/form-data">
        <div class="row">
            <div class="mb-3 col-md-3">
                <label asp-for="Name" class="form-label">Name</label>
                <input asp-for="Name" class="form-control">
                <span asp-validation-for="Name" style="color:red"></span>
            </div>
            <div class="mb-3 col-md-3">
                <label asp-for="Price" class="form-label">Price</label>
                <input asp-for="Price" class="form-control">
                <span asp-validation-for="Price" style="color:red"></span>
            </div>
            <div class="mb-3 col-md-3">
                <label asp-for="SKU" class="form-label">SKU</label>
                <input asp-for="SKU" class="form-control">
                <span asp-validation-for="SKU" style="color:red"></span>
            </div>
            <div class="mb-3 col-md-3">
                <label asp-for="CategoryId" class="form-label">Categories</label>
				<select asp-for ="CategoryId" class="form-control" asp-items="new SelectList(ViewBag.Categories,nameof(Category.Id),nameof(Category.Name))">
                    </select>
                <span asp-validation-for="CategoryId" style="color:red"></span>
            </div>
            <div class="mb-3 col-md-3">
                <label asp-for="ProductImages.FirstOrDefault().MainImageFile" class="form-label">Main Image File</label>
                <input asp-for="ProductImages.FirstOrDefault().MainImageFile" class="form-control">
                <span asp-validation-for="ProductImages.FirstOrDefault().MainImageFile" style="color:red"></span>
            </div>
            <div class="mb-3 col-md-3">
                <label asp-for="ProductImages.FirstOrDefault().HoverImageFile" class="form-label">Hover Image File</label>
                <input asp-for="ProductImages.FirstOrDefault().HoverImageFile" class="form-control">
                <span asp-validation-for="ProductImages.FirstOrDefault().HoverImageFile" style="color:red"></span>
            </div>.
            <div class="mb-3 col-md-3">
                <label asp-for="ProductImages.FirstOrDefault().AdditionImageFile" class="form-label">Addition Image File</label>
                <input asp-for="ProductImages.FirstOrDefault().AdditionImageFile" class="form-control">
                <span asp-validation-for="ProductImages.FirstOrDefault().AdditionImageFile" style="color:red"></span>
            </div>
        </div>
        <div class="row">
            <div class="mb-3 col-md-6">
                <label asp-for="Description" class="form-label">Description</label>
                <textarea class="form-control" asp-for="Description" rows="4"></textarea>
            </div>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div> *@